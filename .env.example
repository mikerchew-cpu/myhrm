# Database
# SQLite (default — no setup needed):
DATABASE_URL="file:./dev.db"

# For Vercel Postgres (production), uncomment:
# DATABASE_URL="postgresql://user:password@host:5432/myhrm?schema=public"
